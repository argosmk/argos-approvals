import { createClient } from "npm:@supabase/supabase-js@2";
import webpush from "npm:web-push@3";
const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!;const SERVICE_ROLE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;const VAPID_PUBLIC_KEY=Deno.env.get("VAPID_PUBLIC_KEY")||"";const VAPID_PRIVATE_KEY=Deno.env.get("VAPID_PRIVATE_KEY")||"";const VAPID_SUBJECT=Deno.env.get("VAPID_SUBJECT")||"mailto:contato@argosmk.com";const supabase=createClient(SUPABASE_URL,SERVICE_ROLE_KEY);
function json(data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json"}})}
function minutesBetween(a:string|null|undefined,b:number){if(!a)return Number.POSITIVE_INFINITY;return Math.max(0,(b-new Date(a).getTime())/60000)}
async function companyNameForTask(organizationId:string,companyId:string|null|undefined){if(!companyId)return null;const{data:ws}=await supabase.from("workspace_state").select("payload").eq("organization_id",organizationId).maybeSingle();const companies=(ws?.payload?.companies??[])as Array<{id:string;name?:string}>;return companies.find(c=>c.id===companyId)?.name||null}
async function taskMeta(organizationId:string,taskId:string|null|undefined){if(!taskId)return null;const{data}=await supabase.from("app_tasks").select("id,title,company_id,responsible_id,status,created_at").eq("organization_id",organizationId).eq("id",taskId).is("deleted_at",null).maybeSingle();return data||null}
async function activeProfile(organizationId:string,profileId:string){const{data}=await supabase.from("profiles").select("id,role,active,last_seen_at,visible_statuses,notification_prefs").eq("organization_id",organizationId).eq("id",profileId).maybeSingle();return data||null}
function profileCanSeeStatus(profile:any,statusId:string|null|undefined){if(!statusId)return true;if(profile?.role==="admin")return true;const visible=Array.isArray(profile?.visible_statuses)?profile.visible_statuses:[];return visible.includes(statusId)}
function statusChangePushEnabled(profile:any){const statuses=profile?.notification_prefs?.statuses;if(typeof statuses?.__all__==="boolean")return statuses.__all__;if(statuses&&typeof statuses==="object"){const legacy=Object.entries(statuses).filter(([key])=>key!=="__all__");if(legacy.length)return legacy.some(([,value])=>value!==false)}return profile?.role!=="client"}
function profileAllowsPushEvent(profile:any,event:string|null|undefined){if(!event)return true;if(event==="Status da tarefa"){const dedicated=profile?.notification_prefs?.push_status;return typeof dedicated==="boolean"?dedicated:statusChangePushEnabled(profile)}const dedicatedEvents=profile?.notification_prefs?.push_events;if(Array.isArray(dedicatedEvents))return dedicatedEvents.includes(event);const legacyEvents=profile?.notification_prefs?.events;return Array.isArray(legacyEvents)?legacyEvents.includes(event):true}
function profileAllowsSystemEvent(profile:any,event:string){const events=profile?.notification_prefs?.events;if(Array.isArray(events))return events.includes(event);return profile?.role!=="client"}
function saoPauloDateKey(date=new Date()){const parts=new Intl.DateTimeFormat("en-US",{timeZone:"America/Sao_Paulo",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(date);const get=(type:string)=>parts.find(part=>part.type===type)?.value||"";return`${get("year")}-${get("month")}-${get("day")}`}
async function deliverySettings(organizationId:string){const{data}=await supabase.from("notification_delivery_settings").select("enabled,team_digest_minutes,client_approval_delay_minutes,presence_grace_seconds").eq("organization_id",organizationId).maybeSingle();return{enabled:data?.enabled!==false,teamDigestMinutes:Number(data?.team_digest_minutes??30),clientApprovalDelayMinutes:Number(data?.client_approval_delay_minutes??5),presenceGraceSeconds:Number(data?.presence_grace_seconds??120)}}
async function markRows(ids:string[],patch:Record<string,unknown>){if(!ids.length)return;await supabase.from("notification_push_queue").update({...patch,updated_at:new Date().toISOString()}).in("id",ids)}
async function sendPayload(organizationId:string,profileId:string,payload:string){const{data:subs}=await supabase.from("push_subscriptions").select("id,endpoint,p256dh,auth_key").eq("organization_id",organizationId).eq("profile_id",profileId);if(!subs?.length)return{sent:0,devices:0};let sent=0;for(const sub of subs){try{await webpush.sendNotification({endpoint:sub.endpoint,keys:{p256dh:sub.p256dh,auth:sub.auth_key}},payload);sent++}catch(err:any){if(err?.statusCode===404||err?.statusCode===410)await supabase.from("push_subscriptions").delete().eq("id",sub.id);else console.error("push send failed",err)}}return{sent,devices:subs.length}}
async function reminderTargets(organizationId:string,task:any,targets:string[]){const ids=new Set<string>();if(targets.includes("task_responsible")&&task?.responsible_id)ids.add(String(task.responsible_id));if(targets.includes("client_approvers")){const{data:ws}=await supabase.from("workspace_state").select("payload").eq("organization_id",organizationId).maybeSingle();const users=(ws?.payload?.users??[])as Array<{id:string;role:string;active?:boolean;companyIds?:string[]}>;for(const user of users){if(user.role==="client"&&user.active!==false&&Array.isArray(user.companyIds)&&user.companyIds.includes(task.company_id))ids.add(String(user.id))}}if(!ids.size)return[];const{data:profiles}=await supabase.from("profiles").select("id,role,active,visible_statuses").eq("organization_id",organizationId).in("id",[...ids]);return(profiles??[]).filter((profile:any)=>profile.active!==false&&profileCanSeeStatus(profile,task?.status)).map((profile:any)=>String(profile.id))}
async function clientProfileIdsForCompany(organizationId:string,companyId:string|null|undefined){const ids=new Set<string>();if(!companyId)return ids;const{data:ws}=await supabase.from("workspace_state").select("payload").eq("organization_id",organizationId).maybeSingle();const users=(ws?.payload?.users??[])as Array<{id:string;role:string;active?:boolean;companyIds?:string[]}>;for(const user of users){if(user.role==="client"&&user.active!==false&&Array.isArray(user.companyIds)&&user.companyIds.includes(companyId))ids.add(String(user.id))}return ids}
async function generateReminderQueue(){const{data:rules,error}=await supabase.from("notification_reminder_rules").select("id,organization_id,status_key,enabled,targets,first_after_minutes,interval_minutes").eq("enabled",true);if(error)throw error;let queued=0;const nowMs=Date.now();for(const rule of rules??[]){const settings=await deliverySettings(rule.organization_id);if(!settings.enabled)continue;const{data:statusRow}=await supabase.from("task_statuses").select("name").eq("organization_id",rule.organization_id).eq("key",rule.status_key).maybeSingle();const statusName=statusRow?.name||rule.status_key;const{data:tasks}=await supabase.from("app_tasks").select("id,title,company_id,responsible_id,status,created_at").eq("organization_id",rule.organization_id).eq("status",rule.status_key).eq("archived",false).is("deleted_at",null);for(const task of tasks??[]){const{data:logs}=await supabase.from("app_task_logs").select("at").eq("organization_id",rule.organization_id).eq("task_id",task.id).eq("type","status").ilike("text",`%para ${statusName}`).order("at",{ascending:false}).limit(1);const enteredAt=logs?.[0]?.at||task.created_at;const elapsedMinutes=Math.floor(minutesBetween(enteredAt,nowMs));const first=Math.max(0,Number(rule.first_after_minutes??1440));if(elapsedMinutes<first)continue;const occurrence=1;const enteredKey=new Date(enteredAt).toISOString();const profileIds=await reminderTargets(rule.organization_id,task,Array.isArray(rule.targets)?rule.targets:["client_approvers"]);for(const profileId of profileIds){const sourceKey=`reminder:${rule.id}:${task.id}:${profileId}:${enteredKey}`;const notificationId=sourceKey;const reminderText=`A tarefa permanece em ${statusName} há ${elapsedMinutes} min.`;const{error:notificationError}=await supabase.from("app_notifications").upsert({organization_id:rule.organization_id,id:notificationId,task_id:task.id,user_id:profileId,text:reminderText,event:"Lembrete de status",status_id:rule.status_key,done:false,at:new Date().toISOString(),payload:{actorId:null,actorName:"Sistema",reminder:true,skip_auto_push:true},updated_at:new Date().toISOString()},{onConflict:"organization_id,id"});if(notificationError){console.error("reminder system notification insert failed",notificationError);continue}const{error:insertError}=await supabase.from("notification_push_queue").insert({organization_id:rule.organization_id,profile_id:profileId,notification_id:notificationId,task_id:task.id,kind:"reminder",event:"Lembrete de status",status_id:rule.status_key,source_key:sourceKey,deliver_after:new Date().toISOString(),payload:{text:reminderText,rule_id:rule.id,occurrence_number:occurrence,status_name:statusName,elapsed_minutes:elapsedMinutes}});if(!insertError)queued++;else if(!String(insertError.message||"").toLowerCase().includes("duplicate"))console.error("reminder queue insert failed",insertError)}}}return queued}
async function generateDeadlineNotifications(){const today=saoPauloDateKey();const yesterday=saoPauloDateKey(new Date(Date.now()-86400000));const{data:finalStatuses}=await supabase.from("task_statuses").select("organization_id,key").eq("final",true);const finalKeys=new Set((finalStatuses??[]).map((row:any)=>`${row.organization_id}:${row.key}`));const{data:tasks,error}=await supabase.from("app_tasks").select("id,organization_id,title,company_id,responsible_id,status,internal_date,archived").eq("archived",false).is("deleted_at",null).not("internal_date","is",null);if(error)throw error;let created=0;for(const task of tasks??[]){const deadline=String(task.internal_date||"").slice(0,10);if(!deadline||deadline>today||deadline<yesterday)continue;if(finalKeys.has(`${task.organization_id}:${task.status}`))continue;const event=deadline===today?"Prazo hoje":"Prazo vencido";const text=deadline===today?"O prazo interno desta tarefa vence hoje.":`O prazo interno desta tarefa venceu em ${deadline}.`;const clientProfileIds=await clientProfileIdsForCompany(task.organization_id,task.company_id);const{data:profiles}=await supabase.from("profiles").select("id,role,active,visible_statuses,notification_prefs").eq("organization_id",task.organization_id).eq("active",true);for(const profile of profiles??[]){const isRecipient=profile.role==="admin"||(profile.role==="team"&&String(profile.id)===String(task.responsible_id||""))||(profile.role==="client"&&clientProfileIds.has(String(profile.id)));if(!isRecipient)continue;if(!profileCanSeeStatus(profile,task.status))continue;const systemEnabled=profileAllowsSystemEvent(profile,event);const pushEnabled=profileAllowsPushEvent(profile,event);if(!systemEnabled&&!pushEnabled)continue;const notificationId=`deadline:${event}:${task.id}:${profile.id}:${deadline}`;const{error:notificationError}=await supabase.from("app_notifications").upsert({organization_id:task.organization_id,id:notificationId,task_id:task.id,user_id:String(profile.id),text,event,status_id:task.status,done:false,at:new Date().toISOString(),payload:{actorId:null,actorName:"Sistema",deadline,pushOnly:!systemEnabled&&pushEnabled},updated_at:new Date().toISOString()},{onConflict:"organization_id,id",ignoreDuplicates:true});if(!notificationError)created++;else if(!String(notificationError.message||"").toLowerCase().includes("duplicate"))console.error("deadline notification insert failed",notificationError)}}return created}
async function processSingle(row:any){const ids=[row.id];const profile=await activeProfile(row.organization_id,row.profile_id);if(!profile||profile.active===false){await markRows(ids,{status:"skipped",skip_reason:"inactive_profile"});return{sent:0,skipped:1}}const settings=await deliverySettings(row.organization_id);if(!settings.enabled){await markRows(ids,{status:"skipped",skip_reason:"delivery_disabled"});return{sent:0,skipped:1}}if(row.status_id&&!profileCanSeeStatus(profile,row.status_id)){await markRows(ids,{status:"skipped",skip_reason:"status_not_visible"});return{sent:0,skipped:1}}if(row.kind!=="reminder"&&!profileAllowsPushEvent(profile,row.event)){await markRows(ids,{status:"skipped",skip_reason:"notification_event_disabled"});return{sent:0,skipped:1}}const lastSeenMs=profile.last_seen_at?new Date(profile.last_seen_at).getTime():0;if(lastSeenMs&&Date.now()-lastSeenMs<=settings.presenceGraceSeconds*1000){await markRows(ids,{status:"skipped",skip_reason:"user_active"});return{sent:0,skipped:1}}const task=await taskMeta(row.organization_id,row.task_id);if(row.kind==="client_approval"&&(!task||task.status!==row.status_id)){await markRows(ids,{status:"cancelled",skip_reason:"task_left_status"});return{sent:0,skipped:1}}if(row.kind==="reminder"){const ruleId=row.payload?.rule_id;const{data:rule}=await supabase.from("notification_reminder_rules").select("enabled,status_key").eq("id",ruleId).maybeSingle();if(!rule?.enabled||!task||task.status!==rule.status_key){await markRows(ids,{status:"cancelled",skip_reason:"reminder_no_longer_valid"});return{sent:0,skipped:1}}}if(!task){await markRows(ids,{status:"cancelled",skip_reason:"task_missing"});return{sent:0,skipped:1}}const companyName=await companyNameForTask(row.organization_id,task.company_id);const title=companyName?`${companyName} • ${task.title}`:task.title;const body=row.kind==="reminder"?`Ainda em ${row.payload?.status_name||row.status_id}.`:String(row.payload?.text||"Há uma atualização aguardando você.");const payload=JSON.stringify({title,body,taskId:task.id,url:`/#/task/${task.id}`});const result=await sendPayload(row.organization_id,row.profile_id,payload);if(result.sent<=0){await markRows(ids,{status:"skipped",skip_reason:"no_subscription"});return{sent:0,skipped:1}}await markRows(ids,{status:"sent",sent_at:new Date().toISOString(),skip_reason:null});if(row.kind==="reminder"&&row.payload?.rule_id){await supabase.from("notification_reminders_log").upsert({organization_id:row.organization_id,rule_id:row.payload.rule_id,task_id:row.task_id,profile_id:row.profile_id,occurrence_number:Number(row.payload?.occurrence_number||1),channel:"push",sent_at:new Date().toISOString()},{onConflict:"rule_id,task_id,profile_id,occurrence_number,channel"})}return{sent:1,skipped:0}}
async function processTeamDigest(rows:any[]){if(!rows.length)return{sent:0,skipped:0};const allIds=rows.map(r=>r.id);const firstRow=rows[0];const profile=await activeProfile(firstRow.organization_id,firstRow.profile_id);if(!profile||profile.active===false){await markRows(allIds,{status:"skipped",skip_reason:"inactive_profile"});return{sent:0,skipped:allIds.length}}const settings=await deliverySettings(firstRow.organization_id);if(!settings.enabled){await markRows(allIds,{status:"skipped",skip_reason:"delivery_disabled"});return{sent:0,skipped:allIds.length}}const eligibleRows:any[]=[];let filtered=0;for(const candidate of rows){if(candidate.status_id&&!profileCanSeeStatus(profile,candidate.status_id)){await markRows([candidate.id],{status:"skipped",skip_reason:"status_not_visible"});filtered++;continue}if(!profileAllowsPushEvent(profile,candidate.event)){await markRows([candidate.id],{status:"skipped",skip_reason:"notification_event_disabled"});filtered++;continue}eligibleRows.push(candidate)}if(!eligibleRows.length)return{sent:0,skipped:filtered};const ids=eligibleRows.map(r=>r.id);const row=eligibleRows[0];const lastSeenMs=profile.last_seen_at?new Date(profile.last_seen_at).getTime():0;if(lastSeenMs&&Date.now()-lastSeenMs<=settings.presenceGraceSeconds*1000){await markRows(ids,{status:"skipped",skip_reason:"user_active"});return{sent:0,skipped:ids.length}}const firstTask=await taskMeta(row.organization_id,row.task_id);let title=`${eligibleRows.length} novas notificações`;let body=eligibleRows.length===1?String(row.payload?.text||"Há uma nova atualização no Argos."):`Você tem ${eligibleRows.length} atualizações pendentes no Argos.`;if(rows.length===1&&firstTask){const companyName=await companyNameForTask(row.organization_id,firstTask.company_id);title=companyName?`${companyName} • ${firstTask.title}`:firstTask.title}const payload=JSON.stringify({title,body,taskId:eligibleRows.length===1?row.task_id:null,url:eligibleRows.length===1&&row.task_id?`/#/task/${row.task_id}`:"/"});const result=await sendPayload(row.organization_id,row.profile_id,payload);if(result.sent<=0){await markRows(ids,{status:"skipped",skip_reason:"no_subscription"});return{sent:0,skipped:ids.length+filtered}}await markRows(ids,{status:"sent",sent_at:new Date().toISOString(),skip_reason:null});return{sent:1,skipped:filtered}}
Deno.serve(async()=>{try{if(!VAPID_PUBLIC_KEY||!VAPID_PRIVATE_KEY)return json({ok:false,error:"missing_vapid_config"},503);webpush.setVapidDetails(VAPID_SUBJECT,VAPID_PUBLIC_KEY,VAPID_PRIVATE_KEY);const queuedReminders=await generateReminderQueue();const deadlineNotifications=await generateDeadlineNotifications();const nowIso=new Date().toISOString();const{data:dueRows,error}=await supabase.from("notification_push_queue").select("*").eq("status","pending").lte("deliver_after",nowIso).order("deliver_after",{ascending:true}).limit(250);if(error)throw error;const rows=dueRows??[];const digestGroups=new Map<string,any[]>();const singles:any[]=[];for(const row of rows){if(row.kind==="team_digest"){const key=`${row.organization_id}:${row.profile_id}`;if(!digestGroups.has(key))digestGroups.set(key,[]);digestGroups.get(key)!.push(row)}else singles.push(row)}let sent=0;let skipped=0;for(const group of digestGroups.values()){const result=await processTeamDigest(group);sent+=result.sent;skipped+=result.skipped}for(const row of singles){const result=await processSingle(row);sent+=result.sent;skipped+=result.skipped}return json({ok:true,queuedReminders,deadlineNotifications,due:rows.length,sent,skipped})}catch(err){console.error(err);const message=err instanceof Error?err.message:(err&&typeof err==="object"?JSON.stringify(err):String(err));return json({ok:false,error:message},500)}});