import React,{useEffect,useMemo,useRef,useState}from'react';
import{RADAR_LANGUAGES,RADAR_SOURCES,RADAR_TTL_DAYS_DEFAULT,briefingQueries,emptyBriefing,itemsForCompany,kindLabel,listToText,loadRadarStore,mergeItems,normalizeSettings,parseList,pruneExpired,runScan,saveRadarStore,sortItems}from'../services/radarService';
import'./radarBeta.css';import'./radarCompact.css';
const MAX_PER_CLIENT=15,MAX_KEYWORDS_SCAN=5;
function fmtWhen(iso){if(!iso)return'sem data';const d=new Date(iso),diff=(Date.now()-d.getTime())/3600000;if(diff<1)return'agora há pouco';if(diff<24)return`há ${Math.round(diff)}h`;const p=n=>String(n).padStart(2,'0');return`${p(d.getDate())}/${p(d.getMonth()+1)}`}
function scoreOf(it,id=''){return id?(it.clients?.[id]?.score||0):Math.max(0,...Object.values(it.clients||{}).map(c=>c.score||0))}
function capItems(items,briefings,savedKeys){const keep=new Map();for(const key of savedKeys){const it=items.find(x=>x.key===key);if(it)keep.set(key,it)}for(const b of briefings){for(const it of sortItems(itemsForCompany(items,b.companyId),b.companyId).filter(x=>scoreOf(x,b.companyId)>=45).slice(0,MAX_PER_CLIENT))keep.set(it.key,it)}return[...keep.values()]}
export default function RadarBetaPanel({companies=[]}){const[store,setStore]=useState(()=>loadRadarStore());const[tab,setTab]=useState('feed');const[view,setView]=useState('todas');const[companyFilter,setCompanyFilter]=useState('');const[editing,setEditing]=useState('');const[scan,setScan]=useState(null);const[message,setMessage]=useState(null);const abortRef=useRef(null);function persist(next){setStore(next);saveRadarStore(next);return next}const settings=useMemo(()=>normalizeSettings(store.settings),[store.settings]);const briefings=useMemo(()=>Object.values(store.briefings||{}),[store.briefings]);const active=briefings.filter(b=>b.active!==false&&briefingQueries(b,settings).length);const companyName=id=>companies.find(c=>c.id===id)?.name||id;const savedKeys=useMemo(()=>new Set((store.saved||[]).map(s=>s.key)),[store.saved]);useEffect(()=>{const kept=pruneExpired(store.items||[],[...savedKeys]);if(kept.length!==(store.items||[]).length)persist({...store,items:kept});return()=>abortRef.current?.abort()},[]);
const visible=useMemo(()=>{let list=itemsForCompany(store.items||[],companyFilter);if(view==='salvos')list=list.filter(it=>savedKeys.has(it.key));return sortItems(list,companyFilter).slice(0,companyFilter?MAX_PER_CLIENT:60)},[store.items,companyFilter,view,savedKeys]);
async function handleScan(){if(!active.length){setMessage({type:'warn',text:'Cadastre o briefing de pelo menos um cliente antes de varrer.'});setTab('briefings');return}const controller=new AbortController();abortRef.current=controller;setMessage(null);setView('todas');setScan({done:0,total:0,label:'preparando'});try{const focused=active.map(b=>({...b,keywords:(b.keywords||[]).slice(0,MAX_KEYWORDS_SCAN),themes:[]}));const result=await runScan(focused,{ttlDays:store.ttlDays||RADAR_TTL_DAYS_DEFAULT,settings,signal:controller.signal,onProgress:p=>setScan(p)});const merged=mergeItems(store.items||[],result.items);const pruned=pruneExpired(merged.items,[...savedKeys]);const curated=capItems(pruned,active,savedKeys);persist({...store,items:curated,lastScanAt:new Date().toISOString()});setMessage({type:'ok',text:`Radar atualizado · ${curated.length} oportunidade(s) mantida(s)`,detail:`máximo de ${MAX_PER_CLIENT} por cliente, priorizando relevância`})}catch(err){setMessage({type:'bad',text:err?.message||String(err)})}finally{setScan(null);abortRef.current=null}}
function toggleSaved(item){const saved=store.saved||[];const next=savedKeys.has(item.key)?saved.filter(s=>s.key!==item.key):[...saved,{key:item.key,title:item.title,url:item.url,savedAt:new Date().toISOString()}];persist({...store,saved:next})}function discard(item){persist({...store,items:(store.items||[]).filter(it=>it.key!==item.key),saved:(store.saved||[]).filter(s=>s.key!==item.key)})}
return <section className="panel-page radar"><header className="radar-head"><h1>Radar de Pautas<span className="radar-beta">beta</span></h1></header>{tab==='feed'&&<div className="radar-compact-nav"><select value={companyFilter} onChange={e=>setCompanyFilter(e.target.value)}><option value="">Todos os clientes</option>{briefings.map(b=><option key={b.companyId} value={b.companyId}>{companyName(b.companyId)}</option>)}</select><div className="radar-compact-tabs"><button className={view==='todas'?'is-on':''} onClick={()=>setView('todas')}>Oportunidades</button><button className={view==='salvos'?'is-on':''} onClick={()=>setView('salvos')}>Salvos</button></div><span className="radar-compact-spacer"/><button onClick={()=>setTab('briefings')}>Briefings</button><button className="primary" disabled={!!scan} onClick={handleScan}>{scan?'Varrendo…':'Atualizar radar'}</button></div>}{tab==='briefings'&&<div className="radar-compact-nav"><b>Configuração do Radar</b><span className="radar-compact-spacer"/><button onClick={()=>setTab('feed')}>Voltar ao feed</button></div>}{scan&&<div className="radar-scan"><div className="radar-scan-top"><b>Varrendo fontes…</b><span>{scan.done}/{scan.total||'?'}{scan.label?` · ${scan.label}`:''}</span><button onClick={()=>abortRef.current?.abort()}>Parar</button></div><div className="radar-progress"><i style={{width:`${scan.total?(scan.done/scan.total)*100:5}%`}}/></div></div>}{message&&<p className={`radar-status is-${message.type}`}><i className="radar-dot"/><span>{message.text}{message.detail?` · ${message.detail}`:''}</span></p>}{tab==='feed'?<Feed items={visible} companyName={companyName} companyFilter={companyFilter} savedKeys={savedKeys} toggleSaved={toggleSaved} discard={discard} lastScanAt={store.lastScanAt}/>:<Briefings companies={companies} store={store} settings={settings} setSettings={next=>persist({...store,settings:next})} persist={persist} editing={editing} setEditing={setEditing}/>}</section>}
function Feed({items,companyName,companyFilter,savedKeys,toggleSaved,discard,lastScanAt}){return <>{lastScanAt&&<p className="radar-compact-note">Última varredura {fmtWhen(lastScanAt)} · mostrando somente as oportunidades mais relevantes</p>}{!items.length?<div className="radar-empty"><h3>Nenhuma oportunidade por aqui.</h3><p>Atualize o radar ou ajuste o briefing deste cliente.</p></div>:<div className="radar-list">{items.map(item=><Card key={item.key} item={item} companyName={companyName} companyFilter={companyFilter} saved={savedKeys.has(item.key)} onSave={()=>toggleSaved(item)} onDiscard={()=>discard(item)}/>)}</div>}</>}
function Card({item,companyName,companyFilter,saved,onSave,onDiscard}){const clients=Object.entries(item.clients||{}).map(([id,data])=>({id,...data})).sort((a,b)=>b.score-a.score);const best=companyFilter?clients.find(c=>c.id===companyFilter)||clients[0]:clients[0];return <article className={`radar-card kind-${item.kind||'noticia'}${saved?' is-saved':''}`}><div style={{minWidth:0}}><div className="radar-card-source"><span className="radar-kind">{kindLabel(item.kind)}</span><span>{item.sourceName||item.sourceType}</span><span className="radar-sep">•</span><span>{fmtWhen(item.publishedAt)}</span></div><a className="radar-card-title" href={item.url} target="_blank" rel="noreferrer">{item.title}</a><div className="radar-tags"><span className="radar-tag is-client">{companyName(best?.id)}<em>Relevância:</em><b>{best?.score||0}</b></span></div><p className="radar-card-why"><span className="radar-why-label">Oportunidade</span><span>{best?.term?<>Relacionado a <b>{best.term}</b></>:'Encontrado em fonte acompanhada'}</span></p></div><div className="radar-card-actions"><button className={saved?'radar-saved':''} onClick={onSave}>{saved?'Salvo ✓':'Salvar pauta'}</button><button className="ghost" onClick={onDiscard}>Descartar</button></div></article>}
function Briefings({companies,store,settings,setSettings,persist,editing,setEditing}){const briefings=store.briefings||{};const sem=companies.filter(c=>c.active!==false&&!briefings[c.id]);function update(id,patch){const atual=briefings[id]||emptyBriefing(id);persist({...store,briefings:{...briefings,[id]:{...atual,...patch,updatedAt:new Date().toISOString()}}})}function remove(id){if(!confirm('Apagar o briefing deste cliente?'))return;const next={...briefings};delete next[id];persist({...store,briefings:next});if(editing===id)setEditing('')}const setSource=(id,on)=>setSettings({...settings,sources:{...settings.sources,[id]:on}});return <><div className="radar-settings"><b>Onde o radar procura</b><small>As fontes valem para todos os clientes.</small><div className="radar-settings-grid">{RADAR_SOURCES.map(s=><label key={s.id} className="radar-check"><input type="checkbox" checked={!!settings.sources[s.id]} onChange={e=>setSource(s.id,e.target.checked)}/>{s.label} <small>({s.hint})</small></label>)}<label className="radar-check" style={{marginLeft:'auto',gap:8}}>Idioma<select value={settings.language} onChange={e=>setSettings({...settings,language:e.target.value})}>{RADAR_LANGUAGES.map(l=><option key={l.id} value={l.id}>{l.label}</option>)}</select></label></div></div><div className="radar-clients"><div className="radar-clients-head"><b>Clientes no radar</b><select value="" onChange={e=>{if(e.target.value){setEditing(e.target.value);update(e.target.value,{})}}}><option value="">+ adicionar cliente</option>{sem.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="radar-clients-list">{Object.values(briefings).map(b=><button key={b.companyId} className={`radar-chip${editing===b.companyId?' is-on':''}`} onClick={()=>setEditing(b.companyId)}>{companies.find(c=>c.id===b.companyId)?.name||b.companyId}{b.active===false?' (pausado)':''}</button>)}</div></div>{editing&&<Editor key={editing} briefing={briefings[editing]||emptyBriefing(editing)} title={companies.find(c=>c.id===editing)?.name||editing} onChange={patch=>update(editing,patch)} onRemove={()=>remove(editing)}/>}</>}
function ListField({label,value,onChange,placeholder}){const[text,setText]=useState(()=>listToText(value));return <label>{label}<textarea rows={2} value={text} onChange={e=>{setText(e.target.value);onChange(parseList(e.target.value))}} placeholder={placeholder}/></label>}
function Editor({briefing,title,onChange,onRemove}){return <div className="radar-editor"><div className="radar-editor-head"><h2>{title}</h2><div className="radar-editor-tools"><label className="radar-switch"><input type="checkbox" checked={briefing.active!==false} onChange={e=>onChange({active:e.target.checked})}/> ativo</label><button className="ghost" onClick={onRemove}>Apagar</button></div></div><div className="radar-form"><label>O que o cliente vende<textarea rows={2} defaultValue={briefing.business} onBlur={e=>onChange({business:e.target.value})}/></label><ListField label={`Palavras-chave prioritárias (use até ${MAX_KEYWORDS_SCAN})`} value={briefing.keywords} onChange={keywords=>onChange({keywords})} placeholder="robótica educacional, ensino de IA, programação para crianças"/></div><p className="radar-note">A varredura usa as 5 primeiras palavras-chave e mantém no máximo 15 oportunidades relevantes por cliente.</p></div>}
